---
description: meta rule
globs: []
alwaysApply: true
---

# Rule files overview

- `default` rules: apply to all projects and environments (global defaults that must be followed everywhere, not only in this repository).
- `project` rules: apply only to this specific project (project-scoped rules).

